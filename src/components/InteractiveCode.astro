---
import SandpackPreview from "./SandpackPreview";

export interface Props {
  html?: string;
  css?: string;
  externalResources?: string[];
}

const { html, css, externalResources } = Astro.props;
---

<SandpackPreview {html} {css} {externalResources} client:load />
