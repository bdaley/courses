---
title: Line Breaks
description: Using line breaks in HTML
order: 9
---

import { LinkCard } from '@astrojs/starlight/components';

import InteractiveCode from '../../../../components/InteractiveCode.astro';
import { Quiz } from 'starlight-quiz/components';

<InteractiveCode
  html={`<p>This is line one.<br>
This is line two.<br>
This is line three.</p>`}
/>

Use `<br>` (a void element) to break a line inside a paragraph. Avoid using multiple `<br>` tags for spacing — use CSS margins instead.

<LinkCard title="Line Breaks" href="/courses/dmd-1070/html-basics/line-breaks-video/" description="Watch on dedicated video page." />

<Quiz id="line-break-spacing-quiz">
What is the correct way to add space between two paragraphs?

- [ ] Add multiple `<br>` elements
- [x] Use CSS margins
- [ ] Add an empty paragraph
- [ ] Add a `<hr>` element
</Quiz>
